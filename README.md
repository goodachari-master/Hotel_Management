# hotel_management
open a new terminal 
1. change the directry to "cd backend"
2. run the code "node server.js"

open a another terminal
1. change the directry to "cd frontend"
2. run the code "python3 -m http.server 8081"

open local web app
past the url "http://localhost:8081/index.html"
